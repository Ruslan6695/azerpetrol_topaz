import React from 'react'
import { Profile } from '../../../proccesses/Profile'

type Props = {}

export const ProfileScreen = (props: Props) => {
    return (
        <>
            <Profile />
        </>
    )
}
