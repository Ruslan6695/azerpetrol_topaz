// primary     — лаймовая кнопка, текст ACCENT.OnLime
// secondary   — стеклянная с рамкой
// elevated    — стеклянная переднего плана (итоговые экраны, успех)
// destructive — сплошная STATE.Destructive
export type TPillButtonVariants =
    | 'primary'
    | 'secondary'
    | 'elevated'
    | 'destructive'

export type TPillButtonSizes = 'lg' | 'md' | 'sm'
