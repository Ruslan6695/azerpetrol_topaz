// Шкала отступов из макета «21 Век». Домножается на SIZES.PX на месте:
//     padding: SPACING.XL * SIZES.PX
export const SPACING = {
    XXS: 3,
    XS: 4,
    SM: 8,
    // Зазор между строками списка и между кнопками в столбце — в макете это
    // всегда 10 (списки цен, контактов, новостей, пары кнопок подтверждения).
    ROW_GAP: 10,
    MD: 12,
    LG: 14,
    XL: 16,
    SCREEN: 20,
    XXL: 24,
    SECTION: 32,
    // Отступ плавающего таб-бара от низа экрана. В макете 18, но там нет
    // системных инсетов: на устройстве к этому значению добавляется
    // insets.bottom, поэтому база сделана меньше макетной.
    TABBAR_BOTTOM: 5,
    // Высота плавающего таб-бара: рамки 1×2 + paddingVertical ряда 8×2
    // + айтем (paddingVertical 2×2 + пилюля 4×2 с иконкой 22 + gap 3
    // + подпись ~13). Нужна, чтобы посчитать отступ контента под баром —
    // см. useBottomMenuClearance.
    TABBAR_HEIGHT: 68,
} as const
