import React from 'react'
import { CheckNetworkWidget } from '../../../widgets/CheckNetworkWidget'

type Props = {}

export const CheckNetworkScreen = (props: Props) => {
    return <CheckNetworkWidget />
}
