// Итоги налива приходят с сервера (fuelling/status/), а не считаются на
// клиенте из fuelOption.price: фактические объём и сумма — источник
// истины сервера (он же источник истины для Топаз).
// Живёт в shared, потому что нужен и процессу, и двум виджетам ветки налива,
// а импорт «снизу вверх» запрещён.
export interface IFuellingTotals {
    volume: number
    sum: number
}
